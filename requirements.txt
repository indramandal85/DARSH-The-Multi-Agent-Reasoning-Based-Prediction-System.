# DARSH Python runtime dependencies
#
# This file is intentionally limited to packages the Python app imports
# or relies on at runtime.
#
# Not included here:
# - Ollama itself: install separately and run `ollama serve`
# - Node.js dependencies: install from `frontend/package.json`
# - PDF text extraction helpers such as `mdls`, `textutil`, or `strings`:
#   these are system tools, not pip packages

# API and app server
flask==3.0.2
flask-cors==4.0.0
requests==2.31.0

# Knowledge graph and semantic memory
networkx==3.2.1
chromadb==0.4.24
sentence-transformers==2.7.0

# Numeric utilities
numpy==1.26.4

# Live news ingestion
feedparser==6.0.11
